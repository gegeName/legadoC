插图的那一下，从被插的那个文件里，把它的 workflow 读出来，写进备注里。写进去的同时，把里面的 base64 换掉，换成指向刚插好的那张图在库里的位置。以后凭这个位置，随时能找回原字节。
想法是，在查看和修改的备注的时候 WebView 编辑器里把 workflow 连图一起渲染出来。难点有两个：第一，编辑器是看 Markdown 的料，workflow 是 JSON，硬塞进去只能 render 得很难看，这是格式不对口。第二，workflow 里没有排版信息，图有多大就占多少行，几张大图往那一堆，又丑又占地方。